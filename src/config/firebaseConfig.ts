// Firebase 설정 파일 (FCM 전용)
export const firebaseConfig = {
  // FCM(Firebase Cloud Messaging)만 사용
  // google-services.json에서 자동으로 설정됨
  
  // 추가 설정이 필요하면 여기에 추가
  // projectId: 'oneday-chat-abbe6',
  // storageBucket: 'oneday-chat-abbe6.firebasestorage.app',
};

export default firebaseConfig;
